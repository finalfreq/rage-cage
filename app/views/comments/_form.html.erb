<%= render 'layouts/errors', object: @comment %>

<%= bootstrap_form_for([@post, @comment]) do |f| %>
  <%= f.text_area :body %>
  <%= f.collection_select(:user_id, User.all, :id, :name) %>
  <%= f.submit data: {disable_with: "Cageing...." }, class: "btn btn-success btn-block" %>
<% end %>
